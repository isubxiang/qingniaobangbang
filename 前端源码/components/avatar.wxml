<view class="avatar">
    <image binderror="_onError" mode="aspectFill" src="{{src}}" wx:if="{{!_showDefault&&src&&src.length}}"></image>
    <view class="iconfont icon-face" style="font-size:{{size}}rpx;color:{{color}};opacity:{{opacity}}" wx:else></view>
</view>
